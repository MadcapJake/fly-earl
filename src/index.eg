require: earlgrey as eg

module.exports = @->
   @filter("earl", fn, { extension = ".eg" }) where fn = (src, opts) ->
      try: eg.Generator{opts or {=}}.generate{eg.Source(src, src.url)}.code
      catch e: throw e
